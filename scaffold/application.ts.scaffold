import { stateMachineInterfaces, BaseState } from "assistant-source";
import { injectable, unmanaged } from "inversify";

@injectable()
export class ApplicationState extends BaseState {
  constructor(@unmanaged() stateSetupSet: stateMachineInterfaces.StateSetupSet) {
    super(stateSetupSet);
  }
}