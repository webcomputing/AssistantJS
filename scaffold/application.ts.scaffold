import { State, BaseState } from "assistant-source";
import { injectable, unmanaged } from "inversify";

@injectable()
export class ApplicationState extends BaseState {
  constructor(@unmanaged() setupSet: State.SetupSet) {
    super(setupSet);
  }
}